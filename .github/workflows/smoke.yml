name: runner smoke test
on: workflow_dispatch  # 手动触发
jobs:
  ping:
    runs-on: [self-hosted, Linux, X64]   # 和设置页 Runner 标签一致
    steps:
      - run: |
          echo "whoami: $(whoami)"
          echo "pwd: $(pwd)"
          echo "ok"
